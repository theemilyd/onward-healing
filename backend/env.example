# Onward Backend Environment Variables

# Claude API Key (get from https://console.anthropic.com/)
CLAUDE_API_KEY=your_claude_api_key_here

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Origins (comma-separated for production)
# ALLOWED_ORIGINS=https://your-app-domain.com,https://www.your-app-domain.com

# Optional: Database connection (if you add user authentication later)
# DATABASE_URL=your_database_connection_string

# Optional: JWT Secret (if you add user authentication later)
# JWT_SECRET=your_jwt_secret_here 